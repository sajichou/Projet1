<%= render partial: "shared/sousmenu", locals: {tabname:"code_promo"}  %>
<div class="row justify-content-center">
	<div class="col-12 col-lg-8">

		<%=form_tag "/pages/code_promo", method:"post" do %>
			<div class="row justify-content-center">
				<fieldset>
					<legend> Si vous disposez d'un code promo, entrez le ici :</legend>
						<div class="col-12 mx-auto">
							<p>
								<label for="code"> Code </label>
								<input type="text" class="form-control text-center" name="code" value='<%=current_user.infouser.code%>'>
							</p>
							<% if (current_user.infouser.code == "LANCEMENT18" and !current_user.infouser.code_used) %>
									<p>Super! Votre code est bien valide :)</p>
							<% elsif (current_user.infouser.code == "LANCEMENT18" and current_user.infouser.code_used) %>
									<p>Ce code n'est malheureusement plus valide.</p>
							<%elsif current_user.infouser.code.present? and current_user.infouser.code != "LANCEMENT18"%>
								<p>Ce code n'est pas valide.</p>
							<%end%>
						</div>
					</div>
					<input type="hidden" id="id" name="id" value='<%=@cour_id%>'/>
					<input type="hidden" id="dispo" name="dispo" value='<%=@dispo%>'/>
					<input id="topics" name="topics" value='<%=@topics%>'/>
					<input  id="objectif" name="objectif" value='<%=@objectif%>'/>
				</fieldset>
				<div class="col-8 col-lg-4 mx-auto">
					<button type="submit" class="btn btn-primary">Envoyer</button>
				</div>
		<% end %>

	</div>
</div>